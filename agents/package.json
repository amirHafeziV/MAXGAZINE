{
  "name": "maxgazine-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous agent pipeline that researches, writes, translates, ranks and publishes MAXGAZINE.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "agents": "tsx src/index.ts",
    "publish-article": "tsx src/index.ts publish-article",
    "update-rankings": "tsx src/index.ts update-rankings",
    "update-forecasts": "tsx src/index.ts update-forecasts",
    "build": "tsx ../build/build.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "fast-xml-parser": "^4.5.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}

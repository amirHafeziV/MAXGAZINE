{
  "name": "maxgazine-build",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static-site build: compiles content/ into per-language HTML and sitemap.xml.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsx ../build/build.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
